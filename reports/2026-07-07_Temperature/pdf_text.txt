AI每日深度科普 · 概念课 2026-07-07

Temperature 采样
温度
为什么同一个 AI 有时像标准答案，有时又变得
发散有创意？

核心一句话：Temperature 的本质，是调节 AI 在“下
一个词”选择时有多保守或多随机。
知识地图位置

推理与输出控制：连接
Token、Softmax、
top-p 与结构化输出

适合读者

高中生能读懂，适合非
技术读者、产品经理、
业务负责人

主题：Temperature（采样温度）

今天要建立的直觉

温度不是智商旋钮，而
是概率分布旋钮

形式：HTML / PDF 科普文档

目录
1. 为什么这个概念重要？
2. 一个直观类比：考试答题与创意写作
3. 工作原理（深入浅出）
4. 关键术语解释

5. 一个真实应用案例
6. 常见误区（非常重要）
7. 总结：3句话讲清核心认知
8. 复习问题

为什么这个概念重要？

很多人用 AI 时会遇到一个奇怪现象：同样的问题，AI 有时回答得很稳，有时表达更
丰富，有时甚至变得不太靠谱。很多人会误以为这是模型“心情不好”，其实背后经
常和一个参数有关： temperature ，也就是采样温度。
大模型生成文字时，并不是一次性写完整段话。它更像一个学生边想边写：先看当
前上下文，然后预测下一个最可能出现的 token。每一步，模型都会给很多候选词
打分。“采样温度”决定的是：它应该几乎只选最高分的词，还是允许低一些但也合
理的词有机会被选中。
它解决的问题：让同一个模型在不同任务中切换输出风格。客服、代码、合
同审查需要稳定；广告标题、故事续写、头脑风暴需要多样。温度就是控制
这种“稳”和“活”的旋钮。
理解它很重要，因为这能帮助普通人建立一个基础 AI 世界观：AI 的输出不是凭空冒
出来的句子，而是一连串概率选择的结果。你看到的“创造力”，很多时候来自概率
分布被调得更平缓；你看到的“稳定”，来自概率分布被调得更尖锐。

一个直观类比：考试答题与创意写作

想象一个学生面对两种任务。
第一种是数学考试：“三角形内角和是多少？”这时老师希望他稳定、准确、不要发
挥。他应该直接写“180 度”，而不是写“也许是一个美丽的几何约定”。这就像低温
模式：最可靠的答案获得压倒性优势。
第二种是语文创意写作：“请写一句关于夜空的开头。”这时如果学生每次都写“夜空
很美”，就太单调了。老师希望他可以换一种说法，比如“城市熄灯后，星星才开始
说话”。这就像高温模式：一些不那么常见但仍然合理的表达，会有更多机会被选
中。
所以，Temperature 不是让 AI 更聪明，也不是让 AI 掌握更多知识。它更像老师给
学生的答题要求：这次要标准答案，还是允许更自由的表达？

图解 1：Temperature 像概率调味旋钮。低温让概率集中，高温让概率更分散。

工作原理（深入浅出）

先记住一个核心画面：AI 每生成一个 token，都要从很多候选 token 里选一个。
Temperature 介入的位置，不是在模型“理解问题”之前，而是在模型已经给候选词
打完分之后。
1 输入提示词

2 模型给候选 token 打分

3 Softmax 把分数变成概率

4 Temperature 调整概率形状

用户给出问题、上下文或任务，例
如“写一句关于星空的句子”。模型
先读懂当前文本。

Softmax 像一个换算器，把高低不
同的分数变成一组加起来等于 1 的
概率。

模型会估计很多候选 token 的可能
性，比如“星空”“真美”“闪烁”“像”
等。分数常被称为 logits。

低温让最高概率更突出，高温让概
率分布更平缓。然后系统再从这组
概率里采样下一个 token。

图解 2：AI 生成一句话的流程。Temperature 在概率分布阶段调节输出的确定性和多样性。

可以用一个简化例子理解。假设 AI 要补全“今天的天气很___”，候选 token 的原始
倾向可能是：“好”最高，“热”也不错，“奇妙”较低。低温会让“好”几乎总是被选中；
高温会让“热”“奇妙”这类较低概率但并非完全不合理的词也有机会出现。
低温：更像标准答案

适合事实问答、代码生
成、分类、信息抽取、结
构化输出。它降低发散
性，但不等于绝对正确。

中温：平衡稳定与自
然

适合普通写作、客服回
复、报告润色。既不太死
板，也不太飘。

高温：更像头脑风暴

适合广告标题、故事、创
意方案。它提高多样性，
也会提高偏题和胡编的风
险。

一个实用判断：当任务的评分标准是“不能错”时，温度应偏低；当任务的评分
标准是“不要千篇一律”时，温度可以适当提高。

关键术语解释
术语

专业解释

白话解释

Temperature

用于调节采样概率分布形状的生成参数，通 控制 AI 回答“稳一点”还是
常较低更确定，较高更多样。
“放开一点”的旋钮。

Token

模型处理文本的基本单位，可能是一个字、 AI 写字时使用的小积木。
词、词片段或符号。

Logits

模型在转成概率前给各候选 token 的原始
分数。

Softmax

把一组分数转换成概率分布的函数，输出值 把“分数表”换成“抽签概率
通常在 0 到 1 之间且总和为 1。
表”。

Sampling

根据概率分布选择下一个 token 的过程。

top-p

也叫核采样，只从累计概率达到某个阈值的 先把最靠谱的一小圈候选留
一组高概率 token 中采样。
下，再从里面抽。

top-k

只保留概率最高的 k 个候选 token 参与采
样。

还没换算成百分比的候选词
分数。

按照概率抽一个词出来。

只让前 k 名候选参加抽签。

Deterministic 在同样输入下尽量得到相同或接近相同输出 希望 AI 别乱变，尽量每次
的性质。
都一样。

一个真实应用案例

假设你在做一个企业客服助手，同一个模型要处理两类任务。
第一类是订单状态判断。用户问：“我的包裹为什么停在中转场？”系统需要 AI 根据
物流轨迹判断问题类型，输出类似 delivery_delay 、 address_issue 、
customs_hold 的分类。这种任务最好低温，因为分类结果要进入工单系统，不
能为了“有创意”而乱选。
第二类是安抚用户的话术。用户已经很着急，客服希望 AI 写一段礼貌、自然、不机
械的解释。这时可以用中等温度，让表达更像真人，而不是每次都输出同一句模
板。
真实做法：同一个产品里，不同步骤可以使用不同温度。信息抽取、工具调
用、结构化输出偏低温；营销文案、标题生成、创意方案可以适当升温。
这也解释了为什么很多 AI 应用不是“调一个全局温度就完事”。一个成熟系统会把任
务拆开：哪些步骤要求可靠，哪些步骤允许变化。Temperature 是工程配置的一部
分，不是万能魔法。

常见误区（非常重要）
误区一：温度越高，AI 越聪明。

错误。高温只是让低概率候选更容易被选中，可能带来新鲜表达，也可能带来跑题和幻
觉。

误区二：温度设为 0，就一定完全相同。

不一定。低温会更稳定，但不同服务端实现、模型版本、并发和其他采样设置仍可能让
结果有差异。

误区三：事实问题也应该高温，这样答案更丰富。

事实问题首先要正确。想要更丰富，应增加上下文、要求引用来源或拆分提问，而不是
单纯升温。

误区四：Temperature 和 top-p 可以随便一起调。

很多 API 文档建议优先调其中一个。两者都在改变采样空间，同时乱调会让输出难以解
释。

误区五：温度能解决模型不知道的问题。

不能。模型缺知识或缺证据时，提高温度只会让它更会编。需要 RAG、工具调用或人工
核验。

误区六：所有模型都支持 Temperature。

也不一定。有些新模型或特定推理接口可能不开放这个参数，或要求用提示词、effort
等其他方式控制行为。

总结：3句话讲清核心认知

1. Temperature 不是智商旋钮，而是概率分布旋钮：它改变 AI 选择下一个
token 时的随机程度。
2. 低温更稳定，适合事实、代码、分类和结构化输出；高温更多样，适合
创意写作和头脑风暴。
3. 想让 AI 更可靠，不能只靠调温度；还要配合清晰提示、证据检索、工
具调用、评估和人工校验。

复习问题
1. 为什么说 Temperature 不是让 AI “更聪明”的开关？
请从“概率分布”和“下一 token 选择”的角度解释。

2. 如果你要让 AI 从合同里提取金额和日期，温度应该偏高还是偏低？为什
么？
请说明这个任务更看重稳定性还是多样性。

3. 为什么写广告标题时可以适当提高温度，但做医疗建议时不应该这样做？
请比较两个任务的错误成本和创造性需求。

参考来源

1. OpenAI API Reference：Responses create - temperature 与 top_p 参数
https://developers.openai.com/api/reference/resources/responses/methods/create

2. OpenAI Agents SDK：ModelSettings 中的 temperature、top_p 与参数支持说明
https://openai.github.io/openai-agents-python/ref/model_settings/
3. Hugging Face Transformers：Text generation parameters - temperature、top_p、
top_k
https://huggingface.co/docs/transformers/en/main_classes/text_generation
4. PyTorch Docs：torch.nn.functional.softmax
https://docs.pytorch.org/docs/2.12/generated/torch.nn.functional.softmax.html
5. Anthropic Docs：Model deprecations - 部分模型的 sampling parameters 支持变化
https://docs.anthropic.com/en/docs/about-claude/model-deprecations
6. Holtzman et al.：The Curious Case of Neural Text Degeneration（nucleus sampling
相关论文）
https://arxiv.org/abs/1904.09751

