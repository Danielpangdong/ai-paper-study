AI每日深度科普

2026-07-04

今日概念

RAG
检索增强生成
为什么真正有用的 AI，往往要先“查资料”再回
答？

核心一句话：RAG 的本质，是让大模型像开卷考试一
样，先检索可信资料，再基于资料生成答案。

知识地图：Embedding → RAG → 向量数据库 → Agent 工具使用

今天承接：上一期的“意思坐标”如何变成可用问答系统

它解决什么

它连接什么

今天要记住

模型不知道私有资料、最

Embedding、向量数据

RAG 能降低幻觉风险，

新资料时，先把相关证据

库、搜索索引、上下文窗

但不能自动保证答案正

找出来。

口和大模型。

确。

目录
01

为什么这个概念重要？

02

一个直观类比：开卷考试

03

工作原理（深入浅出）

04

关键术语解释

05

一个真实应用案例

06

常见误区（非常重要）

07

总结（3句话）

08

复习问题（必须）

PART 01

为什么这个概念重要？
大模型很会组织语言，但它有一个天然限制：它并不会自动知道你公司的内
部制度、今天刚更新的政策、某个客户的最新订单状态，也不一定能准确记
住训练资料里的每个细节。
RAG 解决的就是这个问题：在回答之前，先从外部资料库里找出相关内容，
再把这些内容交给大模型，让它基于资料回答。它像给模型配了一个“可查
资料的书包”。

行业为什么离不开它：企业知识库、AI搜索、客服助手、投研问答、
法律/医疗辅助、内部办公 Copilot，都需要模型回答“我自己的资
料”和“最新资料”，这正是 RAG 的核心场景。

它改变了 AI 产品的边界。没有 RAG，很多应用只能像一个会聊天的百科全
书；有了 RAG，模型才更像一个能读公司资料、查证据、给出处的工作助
手。

PART 02

一个直观类比：开卷考试
想象你参加一场考试。闭卷考试时，你只能靠记忆答题；如果题目问到一本
新教材里的内容，你很可能答错或编一段看起来很像答案的话。
RAG 更像开卷考试。老师允许你带教材，但有两个条件：第一，你要先翻到
正确页；第二，你要读懂材料，而不是把整本书全抄上去。RAG 里的“检
索”负责翻到相关页，“生成”负责把材料整理成自然语言答案。
所以，RAG 的关键不是让模型“变得全知”，而是让它在回答前先看到可信
资料。资料找对了，答案才有基础；资料找错了，模型再会说话也可能把错
资料讲得很流畅。

图解 1：RAG 像开卷考试。先找资料，再基于资料回答，而不是只靠模型记忆。

PART 03

工作原理（深入浅出）
1

2

资料入库

变成可搜索的索引

把文档、网页、手册、FAQ 放进系

每个资料块可以做关键词索引，也可

统。长文档通常先切成小块，方便精

以用 Embedding 变成向量，存入向

确检索。

量数据库。

3

4

用户提问时先检索

把证据交给模型

系统把问题也变成查询，找出最相关

相关资料被放进上下文窗口，模型根

的几段资料，而不是把所有资料都塞

据这些证据组织答案，最好同时给出

给模型。

处。

这里有一个常见误会：RAG 并不等于“让模型联网”。RAG 可以用公开网
页，也可以只用企业内部文档；它的本质是“先检索，再生成”。检索的数
据从哪里来，是另一个工程选择。
真正的生产系统还要处理更多细节：资料是不是最新？用户有没有权限看？
相似度最高的结果是否真的有用？答案有没有引用错误？这些问题决定了一
个 RAG 系统是“演示好看”，还是“工作中可靠”。

图解 2：RAG 的工程流程。资料先入库并建立索引，提问时召回证据，再交给大模型生成回答。

PART 04

关键术语解释
术语

专业解释

白话解释

RAG

Retrieval-Augmented

先查资料，再让 AI 写答

Generation，检索增强生成。

案。

从知识库、搜索索引或数据库中召回相

像在教材里翻到可能有

关内容。

答案的页。

大模型根据问题和检索内容生成自然语

把资料读懂后，用人话

言回复。

讲出来。

把长文档切成较小、可单独检索的片

把一本书拆成一张张知

段。

识卡片。

把文本等内容转换成向量表示，用于语

给每段资料做“意思坐

义相似度检索。

标”。

专门存储和快速检索向量的数据库。

按“意思相近”找资料

Retrieval

Generation

Chunk

Embedding

Vector
Database

Context

的图书馆。

模型一次能看到的输入范围。

Window

Grounding

模型桌面上能摊开的资
料面积有限。

让模型输出基于外部事实来源，而不只

把答案“钉”在资料

依赖参数记忆。

上，减少乱编。

PART 05

一个真实应用案例
假设一个企业客服助手要回答：“我的订单显示到达中转场后两天没有变
化，下一步怎么办？”
如果只靠大模型记忆，它可能给出一段泛泛的安慰话。但 RAG 系统会先去
知识库里找：中转异常处理规则、延误判定标准、客户告知话术、人工工单
触发条件。它可能找到几段真正相关的内部资料，再把它们交给模型。
然后模型生成回答：先说明可能原因，再告诉用户可以等待多久、如何查
询、什么情况需要转人工。更好的系统还会显示引用来源，比如“依据：中
转异常处理规范，第 3.2 条，更新时间 2026-06-15”。

一句话看懂价值：RAG 让 AI 客服不只是“会说话”，而是能围绕企
业自己的资料回答，且有机会把答案追溯到来源。

PART 06

常见误区（非常重要）
误区一：RAG 就是联网搜索。
不是。联网搜索只是资料来源之一。RAG 也可以完全基于企业内部文档、数据
库或离线知识库。

误区二：用了 RAG 就不会幻觉。
不是。RAG 能降低模型凭空编造的概率，但如果检索错、资料旧、提示写得
差，模型仍然可能误读或编造。

误区三：把所有资料都塞给模型最好。
不是。上下文窗口有限，塞太多无关内容会挤掉重点，也会让模型被噪音干扰。

误区四：向量相似度最高就是正确答案。
不一定。语义相近只代表“可能相关”，还要看来源、时间、权限、上下文和业
务规则。

误区五：RAG 可以替代数据库查询。
不能简单替代。订单号、余额、库存这类精确数据，通常需要结构化数据库或工
具调用，而不是只靠相似度搜索。

误区六：RAG 是一次性工程。
不是。好系统要持续评估召回率、答案准确率、引用质量、资料更新和用户反
馈。

PART 07

总结（3句话）
1. RAG 的本质，是让大模型在回答前先检索外部资料，再基于资
料生成答案。
2. 它把 Embedding、向量数据库、搜索索引和大模型连接起
来，是企业 AI 应用落地的关键桥梁。
3. RAG 能减少“凭空编”，但不能自动保证正确；资料质量、检
索质量、权限控制和答案核验同样重要。

PART 08

复习问题（必须）
1. 为什么说 RAG 像“开卷考试”，而不是让模型“突然变聪明”？

2. 如果一个 RAG 系统总是召回不相关资料，最终答案会出现什么问
题？你会优先检查哪几个环节？

3. 为什么订单号、库存数量、账户余额这类精确问题，通常不能只靠向
量检索来回答？

主要参考来源
Lewis et al., Retrieval-Augmented Generation for Knowledge-Intensive NLP Tasks
OpenAI Cookbook: Question answering using embeddings-based search
OpenAI Cookbook: Retrieval augmented generation using Elasticsearch and
OpenAI
Microsoft Learn: RAG and Generative AI - Azure AI Search
Microsoft Learn: Retrieval augmented generation and indexes
Google Cloud: What is Retrieval-Augmented Generation?

