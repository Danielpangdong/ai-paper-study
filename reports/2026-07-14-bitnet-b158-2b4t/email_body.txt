今天精选：BitNet b1.58 2B4T。

它的关键不是“把训练好的模型压成 1-bit”，而是从训练开始就把线性层权重约束为 −1、0、+1 三档。论文在同尺寸 1B–2B 对照中报告：非嵌入权重内存为 0.4 GB，CPU 解码为 29 ms/token；但 MMLU、HumanEval+ 等任务仍落后于 Qwen2.5 1.5B。

附件是一份证据化中文深度解读：逐表核对了数字、说明了不同推理框架带来的混杂因素，并将论文事实与产业推断分开。

核心判断：低比特原生训练已是值得实测的部署路线，但不是“所有大模型从此都能 1-bit”的证明。
